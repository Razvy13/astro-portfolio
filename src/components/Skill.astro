---
const { iconClass, skillDescription } = Astro.props;
---

<div
  class="flex flex-col w-24 items-center p-2 transition-all hover:-translate-y-2 hover:text-neutral-100"
>
  <i class={`${iconClass} mb-2 text-5xl`}></i>
  <p>{skillDescription}</p>
</div>
