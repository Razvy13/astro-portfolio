---
import skills from "@/data/skills";
import Skill from "./Skill.astro";
---

<div class="flex flex-row flex-wrap justify-center gap-2">
  {
    skills.map((skill) => (
      <Skill iconClass={skill.iconClass} skillDescription={skill.name} />
    ))
  }
</div>
